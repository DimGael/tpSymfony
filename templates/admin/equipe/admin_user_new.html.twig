{% extends 'admin/base.html.twig' %}


{% block body %}
    <main>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}

        <a href="{{ path("admin_user_list") }}">Accueil</a>
    </main>
{% endblock %}

{% block title %}
Nouvel Utilisateur
{% endblock %}

