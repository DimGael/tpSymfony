{% extends 'admin/base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ commande.date ? commande.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>PrixTotal</th>
                <td>{{ commande.prixTotal }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ commande.status }}</td>
            </tr>
        </tbody>
    </table>

    <a role="button" class="btn btn-primary" href="{{ path('commande_index') }}">back to list</a>

    <a role="button" class="btn btn-primary" href="{{ path('commande_edit', {'id': commande.id}) }}">edit</a>

    {{ include('admin/commande/_delete_form.html.twig') }}
{% endblock %}
